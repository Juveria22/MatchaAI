# direct deps only, pip resolves the rest
# the old pinned freeze could not install: llama-index-llms-openai 0.6.6 wants
# openai<2 while langchain-openai 1.1.10 wants openai>=2.20

# api
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
pydantic>=2.7

# chat engine, openai first with ollama as the local fallback
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-openai>=0.2
langchain-ollama>=0.2

# doc engine, semantic search over data/
llama-index>=0.12
llama-index-embeddings-openai>=0.3
llama-index-llms-openai>=0.3
llama-index-embeddings-ollama>=0.5
llama-index-llms-ollama>=0.5
